{% extends 'base.html.twig' %}
{% block title %}{{ titulo }}{% endblock %}
{% block body %}
<div class="container">
    <h2>{{ titulo }}</h2>
    <p></p>
     <div> 
        <a href="{{ path('client_index') }}" class="btn btn-primary">Inicio</a>
    </div>
    <p></p>
    <div class="alert alert-success" role="alert">
        {{ msg }}
    </div>
    {{ form(form) }}
</div>
{% endblock %}